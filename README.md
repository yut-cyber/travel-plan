# nimbus-workspace

Personal web playground. Static site, no build step.

- `index.html` — sign-in page demo
- `plan.html` — small tool demo (local storage)

> Note: the tool's data is stored in the browser only; a `data.json` blob may appear in this repo from time to time — it holds nothing readable.
